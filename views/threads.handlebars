<div class="text-center">
  <p>{{{get_emoji}}}</p>
  <h2>{{name}}</h2>
  <p>Created by {{user.name}} on 
    <span for="img" aria-label="calendar">📅</span>
    {{format_date date_created}}
  </p>
  <p>Category: {{category}}</p>
  <p>{{description}}</p>

  <!-- Comment section -->
  <div class="comments-section">
    <h3>Comments</h3>
    <ul class="comments-list">
      <!-- Comment items will be added here dynamically -->
    </ul>
    <div class="comment-form">
      <textarea id="comment-textarea" class="form-control" rows="3" placeholder="Write your comment"></textarea>
      <button id="submit-comment-button" class="btn btn-success">Submit</button>
    </div>
  </div>
</div>

<script src="/js/threadsPage.js"></script>